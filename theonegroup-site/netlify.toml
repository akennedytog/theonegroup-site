[build]
  base = "/theonegroup-site"
  publish = "dist"
  command = "npm run build"

[context.production]
  base = "/theonegroup-site"
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200